#include "utils.hpp"

const int windowWidth = 800;
const int windowHeight = 600;
const int rows = 64;
const int cols = 64;
const int space = 2;

const int cellWidth = (windowWidth / cols)- space;
const int cellHeight = (windowHeight / rows)-space;
